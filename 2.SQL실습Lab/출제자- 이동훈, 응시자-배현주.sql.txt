제시자 : 이동훈, 응시자 : 배현주, 여운중

1. 모든 사원들의 평균 연봉을 출력하시오. 
 select AVG(sal) from emp;

2. 부서별 사원수를 출력하시오.
select count(ename) from emp group by deptno;

3. 직원 수가 3명인 부서에 속한 사원번호와 직원이름을 출력하시오.
select empno, ename from emp group by deptno  in (select deptno from emp group by deptno having count(empno) =3);  ;

4-1. 상사의 직업이 MANAGER인 사원의 이름을 구하시오.
select ename from emp where mgr in(select empno from emp where job = 'MANAGER');
select e2.ename fro emp e1, emp e2 where e1.enpno = e2.mrg and e2.job = 'MANAGER';


4-2.'ACCOUNTING' 부서에 근무중인 사원의 이름을 구하시오.
select e.ename from emp e, dept d where d.dname ='ACCOUNTING';

4-3. 상사의 직업이 MANAGER인 사원들의 연봉의 합을 출력하시오.
select sum(sal) from emp where where mgr in (select empno from emp where job='MANAGER'); 


5. 'ACCOUNTING'부서에 속한 직원들의 모든 정보를 출력하시오.
select * from emp e, dept d where d.dname ='ACCOUNTING';





//select ename from emp e,dept d where mgr in (select empno from emp where job='MANAGER'); 